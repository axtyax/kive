

pub struct Document {
    pub words: Vec<Word>,
}

impl Document {
    pub fn length(&self) -> i32 {
        self.length()
    }
}

pub struct Query {
    pub words: Vec<Word>,
}

impl Query {
    pub fn length(&self) -> i32 {
        self.length()
    }
}

pub struct Word {
    pub data: String,
}
